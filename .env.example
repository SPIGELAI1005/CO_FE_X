# Copy to .env and fill in values from your Supabase project dashboard.
# Never commit .env — the publishable (anon) key is safe for client bundles,
# but keeping secrets out of git is still required for service-role keys.

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key

# Server-only (optional — used by SSR / admin server functions)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_PUBLISHABLE_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# App URL for Stripe redirect URLs (server)
APP_URL=http://localhost:3000

# Stripe (server-only — never expose secret/webhook keys to the client)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_PRO=price_...
STRIPE_PRICE_CAMPAIGN_BOOST=price_...

# Client feature flag — enable partner checkout UI
VITE_FEATURE_STRIPE=false
