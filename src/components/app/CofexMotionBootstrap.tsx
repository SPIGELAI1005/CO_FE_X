import { useParallax } from "@/hooks/use-parallax";

/** Client-only parallax for the marketing landing page ([data-parallax]). */
export function CofexMotionBootstrap() {
  useParallax();
  return null;
}
